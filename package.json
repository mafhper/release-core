{
  "name": "@mafhper/release-core",
  "version": "1.1.5",
  "private": true,
  "description": "O protocolo de release reutilizável para GitHub Actions.",
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}